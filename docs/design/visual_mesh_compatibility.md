# 任务 3 实测兼容性

2026-09-10；三处独立解释器运行 scripts/probe_mesh_compile.py。
证据见同目录 compile_*.json，记录加载路径、错误与显式惯量。

| 几何 | 3.4.0 默认 | 3.4.0 shell | 3.2.3 默认 | 3.2.3 shell |
|---|---|---|---|---|
| 闭合箱体 | 编译+forward通过 | 通过 | 通过 | 语法不支持 |
| 六面六材质箱体 | 体积错误 | 通过 | 通过 | 语法不支持 |
| 单三角形 | 少于4顶点 | 少于4顶点 | 少于4顶点 | 语法不支持 |
| 平面视觉+独立碰撞 | 体积错误 | 通过 | 通过 | 语法不支持 |

所有成功用例质量2、主惯量[1,1,1]不变。以上不代表渲染、接触或真实宿主合并通过。
单三角形暂时明确不支持；未实施细分，不加厚、封孔或删除 primitive。
3.4.0 转换基线可用默认闭合 mesh；平面可选 shell，但必须标明仅在3.4.0验证。
目标真实宿主尚未由用户选定，host_compatibility=pending。两处已安装版本仅代表受测候选。
下一步任务4原生渲染才验证纹理与实际效果；任务6另测非默认宿主 compiler 冲突。
